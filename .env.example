# Database
# Matches docker-compose defaults — update if using an external DB
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/monthly_giving

# Stripe
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
